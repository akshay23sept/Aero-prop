clear all;
close all;
data = importdata('ss2.5.txt') ;
%data = importdata('ss2.5.txt') ;
x = data(:,2) ;
Fs = 2880; 
L = 2880;
y=x-mean(x)
NFFT = 2^nextpow2(L);
Y = fft(y,NFFT)/L;
f = Fs/2*linspace(0,1,NFFT/2+1);
plot(f,2*abs(Y(1:NFFT/2+1)));
grid on
xlabel('Frequency (Hz)')
ylabel('|Y(f)|')
